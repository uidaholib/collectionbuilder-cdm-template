---
# metadata for table ajax
---
{%- assign items = site.data[site.data.theme.metadata] -%}
{ "objects": [
{%- for item in items -%}[ {{ item.title | jsonify }}, {{ item.date | jsonify }}, {{ item.subject | split: ';' | jsonify }}, {{ item.description | truncatewords: 20 | replace: '""','"' | jsonify }}, {{ "/items/" | relative_url | append: item.object-id | append: ".html" | jsonify }} ]{% unless forloop.last %},{% endunless %}
{% endfor %}
] }